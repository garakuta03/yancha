# アカウント・ID台帳

- 最終更新: 2026-07-16
- 位置づけ: **運用で使うIDの一覧**。日付を付けないファイル名にしてあるのは、常に最新を保つ生きた台帳だから。
- 関連: [TODO.md](../../TODO.md) フェーズP / Issue #8

> 🔴 **このファイルに秘密情報を書かないこと。**
> APIキー・クライアントシークレット・リフレッシュトークン・パスワード・バックアップコードは
> **すべて `.env`**（`.gitignore` 済み）へ。ここに書いてよいのは**公開しても困らない識別子だけ**。

---

## Google アカウント

| 項目 | 値 |
|---|---|
| **運用アカウント** | `hakoniwa.scene@gmail.com` |
| 表示名 | `KENTA`（⚠️ ブランドアカウント運用のため**視聴者には見えない**。変更可） |
| 運営主体 | **個人**（`ilks.jp` とは意図的に分離） |
| 再設定用メール | **空欄**（🗣 要再検討 → [セキュリティ設定](2026-07-16-運用アカウントのセキュリティ設定.md) §4） |
| 再設定用電話 | 専用MVNO回線（⚠️ **失効させないこと**。番号はここに書かない） |

> `hakoniwa.studio` は取得済みで埋まっていたため `hakoniwa.scene` を採用。
> ⚠️ **Gmailはピリオドを無視する**ので `hakoniwa.scene` = `hakoniwascene`。

## YouTube チャンネル

### ✅ 本命（ブランドアカウント）— **こちらを使う**

| 項目 | 値 |
|---|---|
| **チャンネルID** | **`UC5rnnlWXI8713Ue-y_iMy1A`** ← **不変。外部リンクはこれを使う** |
| チャンネル名 | `hakoniwa2`（**仮**。ニッチ確定後に変更する） |
| ハンドル | `@hakoniwa-n2y`（**仮**） |
| ⚠️ 名前変更の残枠 | **残り1回 / 14日**（2026-07-16 に1回使用）。ニッチ確定時まで温存する |
| 種別 | **ブランドアカウント**（`myaccount.google.com/brandaccounts` で確認済み） |
| URL | https://www.youtube.com/channel/UC5rnnlWXI8713Ue-y_iMy1A |
| 電話確認 | ✅ 済（**中級者向け機能=有効** → 15分超の動画・カスタムサムネイル・ライブ配信） |
| 上級者向け機能 | 「利用資格あり」（未取得。現時点で不要） |

### ⚠️ 使わない（個人チャンネル・意図せず作成）

| 項目 | 値 |
|---|---|
| チャンネルID | `UCZFDCNxThCY--hhJs4d8M9g` |
| チャンネル名 | `hakoniwa` → **`zz-unused-personal` へ改名予定**（区別のため） |
| ハンドル | `@hakoniwa-f7h` |
| 状態 | **空・放置**（動画0・登録者0） |
| 名前変更の残枠 | 2回 / 14日（未使用） |

> ⚠️ **改名は「個人チャンネル側」を変えること。** ブランド側を戻すと、残り1回を使い切って
> 14日間ロックされる上、両方 `hakoniwa` に逆戻りして意味がない。
> 名前変更の枠は**チャンネルごとに独立**している。

> **なぜ残っているか**: 新規アカウントの1つ目のチャンネルは個人チャンネルになる仕様のため
> （公式に記載なし）。2つ目でブランドアカウントになった。
> **なぜ削除しないか**: 「個人チャンネルを削除してもブランドアカウントは無傷」と書いた**公式文書がなく**、
> **削除の復元・猶予期間の記述も一切ない**。放置コストはゼロなので、ゼロと「不明」ならゼロを取る。
> → [チャンネル開設の実地メモ](2026-07-16-youtubeチャンネル開設の実地メモ.md) §4

### 🔴 2つあることによる事故リスク

**アップロード先・OAuth認可先を取り違えると、パイプラインが個人チャンネルに投稿する。**
公式が「APIを使うアプリは間違ったチャンネルにサインインし得る」と名指しで警告している。

**唯一の確実な防御**（#27 に実装する）:
```
channels.list?part=id,snippet&mine=true
→ items[0].id === "UC5rnnlWXI8713Ue-y_iMy1A" を検証する
```

## Google Cloud

| 項目 | 値 |
|---|---|
| プロジェクト名 | （**要記入**） |
| プロジェクトID | （**要記入**） |
| 有効化API | YouTube Data API v3 |
| **APIキー** | 名前 `youtube-master` / **値は `.env` の `YOUTUBE_API_KEY`** |
| APIキーの制限 | API制限: YouTube Data API v3 のみ / アプリケーション制限: **なし** |
| OAuthクライアント | **未作成**（#27 の実装時に作る） |
| OAuth同意画面 | **未設定**（⚠️ **In production にすること**。Testing だと refresh token が7日で失効） |

> アプリケーション制限が「なし」なのは**公式推奨からの意図的な逸脱**。選択肢が
> Websites/IP/Android/iOS しかなく、ローカル実行のTSスクリプトに合わないため。
> 将来ローカルサーバーから固定IPで回すなら IP制限に切り替える価値がある。
> → [GCP設定](2026-07-16-gcp-youtube-api設定.md)

## GitHub

| 項目 | 値 |
|---|---|
| リポジトリ | `garakuta03/yancha` |

---

## 秘密情報の置き場所（値は書かない）

すべて **`.env`**（`.gitignore` 済み）。`.env.example` にキー名の一覧がある。

| 環境変数 | 用途 | 状態 |
|---|---|---|
| `YOUTUBE_API_KEY` | research の観測（公開データ読み取り） | ✅ 設定済 |
| `GEMINI_API_KEY` | LLM（scene / metadata 生成） | — |
| `YOUTUBE_CLIENT_ID` / `YOUTUBE_CLIENT_SECRET` / `YOUTUBE_REFRESH_TOKEN` | upload（#27） | 未作成 |

⚠️ **`.env` は自動で読み込まれない。** `package.json` の各スクリプトに
`tsx --env-file-if-exists=.env` を付けてある（**Node 20.18+ が必要**）。

## 物理保管

| もの | 場所 |
|---|---|
| バックアップコード（10個・8桁） | 印刷して保管（⚠️ **このアカウントのGmail/Driveには置かない**） |

⚠️ **バックアップコードを再生成すると古いセットが即無効になる。** 紙を必ず差し替えること。
