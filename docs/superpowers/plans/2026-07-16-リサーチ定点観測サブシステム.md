# リサーチ定点観測サブシステム 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 手動シードした競合チャンネルをコマンド駆動でスナップショット取得・SQLite蓄積し、経過時間で正規化したエッジ指標をオンデマンドWebビューで提示するリサーチサブシステムを構築する。

**Architecture:** 既存の root 単一パッケージ（生成パイプライン）を monorepo 化する。汎用ユーティリティを `packages/core`（`@yancha/core`）へ昇格し、リサーチ機能を `packages/research`（`@yancha/research`）に独立パッケージとして実装する。リサーチは `ledger(YAML) → collect → store(SQLite) → report/serve` の直列ステージで、各ステージは tsx で単体実行・vitest で単体テスト可能。常駐なし・コマンド駆動。

**Tech Stack:** TypeScript(ESM) / tsx / vitest / better-sqlite3 / Hono + @hono/node-server / yaml / Node20 global fetch / pnpm workspace

## Global Constraints

- 言語: TypeScript、ESM（`"type": "module"`）。import は拡張子 `.js` を明示する（既存基盤の慣習）。
- テスト: vitest。globals 有効（`describe`/`test`/`expect` を import しない）。
- コード内コメント・ログ・エラーメッセージは**日本語**。
- エラーは `@yancha/core` の `YanchaError`（`code` 付き）で送出する。
- コミットメッセージは日本語 `prefix: 要約`（prefix: feat/fix/docs/style/refactor/test/chore）。
- **`search.list` を使わない**（1ユニット系 API のみ: `channels.list`/`playlistItems.list`/`videos.list`）。
- スクレイピング禁止。公式 YouTube Data API v3 のみ。
- 常駐・cron・Docker はフェーズ0では作らない。すべてオンデマンドのコマンド駆動。
- 各スナップショットに**実測タイムスタンプ**を必ず保存する。「日次」を前提にしない。
- storage は `ResearchStore` インターフェース経由でのみ触る（将来 MariaDB 移行のため）。
- Node は `>=20`（global `fetch` 前提）。
- パッケージ名スコープは `@yancha/*`。

---

## ファイル構成

新規パッケージ `packages/core`:
- `packages/core/package.json` — `@yancha/core`
- `packages/core/tsconfig.json`
- `packages/core/src/errors.ts` — 既存 `src/errors.ts` を移設
- `packages/core/src/logger.ts` — 既存 `src/logger.ts` を移設
- `packages/core/src/json.ts` — 既存 `src/io/json.ts` を移設
- `packages/core/src/index.ts` — 再エクスポート

新規パッケージ `packages/research`:
- `packages/research/package.json` — `@yancha/research`、`@yancha/core` に依存
- `packages/research/tsconfig.json`
- `packages/research/src/config.ts` — リサーチ用 env 設定（`YOUTUBE_API_KEY` 等）
- `packages/research/src/paths.ts` — 台帳/スナップショット/DB のパス解決
- `packages/research/src/types.ts` — ドメイン型（LedgerEntry / ChannelSnapshot / VideoSnapshot / 指標）
- `packages/research/src/ledger.ts` — 台帳の読み書き（YAML）
- `packages/research/src/youtube.ts` — YouTube Data API v3 クライアント
- `packages/research/src/collect.ts` — スナップショット取得（生JSON書き出し＋store追記）
- `packages/research/src/store.ts` — `ResearchStore` インターフェース ＋ SQLite 実装
- `packages/research/src/metrics.ts` — 経過時間正規化のエッジ指標計算
- `packages/research/src/report.ts` — DB からレポート用集計を組み立て
- `packages/research/src/server.ts` — Hono アプリ（JSON API）
- `packages/research/src/web/index.html` — 1画面のWebビュー（Chart.js CDN）
- `packages/research/src/cli.ts` — `add` / `collect` / `serve` コマンド
- テスト: `packages/research/tests/*.test.ts`

既存 root パッケージの変更:
- `src/errors.ts` / `src/logger.ts` / `src/io/json.ts` を削除し、`@yancha/core` から import するよう全 import を張り替え。
- root `package.json` に `@yancha/core` を workspace 依存として追加。

---

## Task 1: `@yancha/core` パッケージへ共有部を昇格

**Files:**
- Create: `packages/core/package.json`, `packages/core/tsconfig.json`, `packages/core/src/errors.ts`, `packages/core/src/logger.ts`, `packages/core/src/json.ts`, `packages/core/src/index.ts`
- Modify: root `package.json`（依存追加）, `src/cli.ts` ほか `errors`/`logger`/`io/json` を import する全ファイル
- Delete: `src/errors.ts`, `src/logger.ts`, `src/io/json.ts`
- Test: 既存 `tests/*.test.ts` がそのまま緑になること

**Interfaces:**
- Produces: `@yancha/core` から `YanchaError`, `StageError`, `toErrorMessage`, `ErrorCode`, `Logger`, `LogLevel`, `readJson`, `writeJson` を named export。

- [ ] **Step 1: `packages/core/package.json` を作成**

```json
{
  "name": "@yancha/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.12",
    "typescript": "^5.5.4"
  }
}
```

- [ ] **Step 2: `packages/core/tsconfig.json` を作成**

root の `tsconfig.json` を確認し、同じ `compilerOptions` を継承する。root が `extends` 可能な base を持たない場合は以下を置く:

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "noEmit": true
  },
  "include": ["src"]
}
```

（既存 root `tsconfig.json` に合わせて値を調整すること。矛盾する場合は root を優先。）

- [ ] **Step 3: 既存の共有モジュールを移設**

`src/errors.ts` の内容を `packages/core/src/errors.ts` へ、`src/logger.ts` を `packages/core/src/logger.ts` へ、`src/io/json.ts` を `packages/core/src/json.ts` へそのままコピーする（中身は変更しない）。

- [ ] **Step 4: `packages/core/src/index.ts` を作成**

```typescript
export { YanchaError, StageError, toErrorMessage } from "./errors.js";
export type { ErrorCode } from "./errors.js";
export { Logger } from "./logger.js";
export type { LogLevel } from "./logger.js";
export { readJson, writeJson } from "./json.js";
```

- [ ] **Step 5: root `package.json` に依存を追加**

`dependencies` に `"@yancha/core": "workspace:*"` を追加する（`dependencies` が無ければ新設）。

- [ ] **Step 6: root の import を張り替え**

`src/` 配下で `./errors.js` / `./logger.js` / `../errors.js` / `./io/json.js` 等を import している全箇所を `@yancha/core` からの import に変更する。例（`src/cli.ts`）:

```typescript
import { YanchaError, toErrorMessage, Logger } from "@yancha/core";
```

`src/io/json.ts` を使っていた `src/stages/*.ts` 等は `import { writeJson } from "@yancha/core";` に変更する。

- [ ] **Step 7: 旧ファイルを削除**

`src/errors.ts`, `src/logger.ts`, `src/io/json.ts` を削除する（`src/io/` が空になれば `src/io/files.ts` の有無を確認し、空ならディレクトリごと削除）。`src/io/files.ts` が残る場合はディレクトリを残す。

- [ ] **Step 8: 依存をインストール**

Run: `pnpm install`
Expected: `@yancha/core` が workspace としてリンクされる。

- [ ] **Step 9: 既存テストと型チェックが緑であることを確認**

Run: `pnpm test && pnpm build`
Expected: 既存の `tests/config.test.ts` `tests/paths.test.ts` `tests/scriptPolicy.test.ts` `tests/scriptStage.test.ts` が全て PASS、`tsc --noEmit` がエラーなし。

- [ ] **Step 10: Commit**

```bash
git add -A
git commit -m "refactor: 共有ユーティリティを@yancha/coreパッケージへ昇格"
```

---

## Task 2: `@yancha/research` パッケージ雛形と設定・型・パス

**Files:**
- Create: `packages/research/package.json`, `packages/research/tsconfig.json`, `packages/research/vitest.config.ts`, `packages/research/src/config.ts`, `packages/research/src/types.ts`, `packages/research/src/paths.ts`
- Test: `packages/research/tests/config.test.ts`, `packages/research/tests/paths.test.ts`

**Interfaces:**
- Produces:
  - `ResearchConfig`: `{ youtubeApiKey: string; youtubeBaseUrl: string; dataDir: string; logLevel: LogLevel }`
  - `loadResearchConfig(env?): ResearchConfig`
  - 型 `LedgerEntry`, `ChannelSnapshot`, `VideoSnapshot`, `SnapshotBatch`
  - `resolveResearchPaths(config): { dataDir; ledgerYaml; snapshotsDir; dbFile }`
  - `snapshotDirFor(config, capturedAt: string): string`

- [ ] **Step 1: `packages/research/package.json` を作成**

```json
{
  "name": "@yancha/research",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit",
    "research": "tsx src/cli.ts"
  },
  "dependencies": {
    "@yancha/core": "workspace:*",
    "better-sqlite3": "^11.3.0",
    "hono": "^4.6.0",
    "@hono/node-server": "^1.13.0",
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^20.14.12",
    "tsx": "^4.16.2",
    "typescript": "^5.5.4",
    "vitest": "^4.1.10"
  }
}
```

- [ ] **Step 2: `packages/research/tsconfig.json` と `vitest.config.ts` を作成**

`tsconfig.json` は Task1 の core と同じ方針。`vitest.config.ts` は root の `vitest.config.ts` を確認し globals 有効設定を踏襲:

```typescript
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    globals: true
  }
});
```

- [ ] **Step 3: `paths.test.ts` の失敗テストを書く**

```typescript
import { resolve } from "node:path";
import { resolveResearchPaths, snapshotDirFor } from "../src/paths.js";
import type { ResearchConfig } from "../src/config.js";

const config = {
  youtubeApiKey: "dummy",
  youtubeBaseUrl: "https://www.googleapis.com/youtube/v3",
  dataDir: "research-data",
  logLevel: "info"
} satisfies ResearchConfig;

describe("resolveResearchPaths", () => {
  test("dataDir配下の主要パスを解決する", () => {
    const paths = resolveResearchPaths(config);
    expect(paths.ledgerYaml).toBe(resolve("research-data", "ledger.yaml"));
    expect(paths.snapshotsDir).toBe(resolve("research-data", "snapshots"));
    expect(paths.dbFile).toBe(resolve("research-data", "data.db"));
  });

  test("snapshotDirForはタイムスタンプでコロンを除去したディレクトリを返す", () => {
    const dir = snapshotDirFor(config, "2026-07-16T09:30:00.000Z");
    expect(dir).toBe(resolve("research-data", "snapshots", "2026-07-16T093000.000Z"));
  });
});
```

- [ ] **Step 4: テストが失敗することを確認**

Run: `pnpm --filter @yancha/research test paths`
Expected: FAIL（`../src/paths.js` が存在しない）

- [ ] **Step 5: `src/config.ts` を実装**

```typescript
import { YanchaError } from "@yancha/core";
import type { LogLevel } from "@yancha/core";

export interface ResearchConfig {
  readonly youtubeApiKey: string;
  readonly youtubeBaseUrl: string;
  readonly dataDir: string;
  readonly logLevel: LogLevel;
}

const logLevels = new Set<LogLevel>(["debug", "info", "warn", "error"]);

export function loadResearchConfig(env: NodeJS.ProcessEnv = process.env): ResearchConfig {
  const youtubeApiKey = env.YOUTUBE_API_KEY?.trim() ?? "";
  if (youtubeApiKey.length === 0) {
    throw new YanchaError("CONFIG_MISSING", "環境変数 YOUTUBE_API_KEY が未設定です。YouTube Data API v3 のキーを設定してください。");
  }
  const logLevel = (env.YANCHA_LOG_LEVEL?.trim() ?? "info") as LogLevel;
  if (!logLevels.has(logLevel)) {
    throw new YanchaError("CONFIG_INVALID", `環境変数 YANCHA_LOG_LEVEL の値が不正です: ${logLevel}`);
  }
  return {
    youtubeApiKey,
    youtubeBaseUrl: env.YOUTUBE_BASE_URL?.trim() || "https://www.googleapis.com/youtube/v3",
    dataDir: env.YANCHA_RESEARCH_DIR?.trim() || "research-data",
    logLevel
  };
}
```

- [ ] **Step 6: `src/types.ts` を実装**

```typescript
// 台帳エントリ（人間が手編集）
export interface LedgerEntry {
  readonly channelId: string;
  readonly note: string;
  readonly tags: readonly string[];
}

// チャンネル単位のスナップショット（取得時点の統計）
export interface ChannelSnapshot {
  readonly channelId: string;
  readonly title: string;
  readonly subscriberCount: number;
  readonly viewCount: number;
  readonly videoCount: number;
  readonly capturedAt: string; // 実測ISO8601
}

// 動画単位のスナップショット
export interface VideoSnapshot {
  readonly videoId: string;
  readonly channelId: string;
  readonly title: string;
  readonly publishedAt: string;
  readonly durationSeconds: number;
  readonly viewCount: number;
  readonly tags: readonly string[];
  readonly capturedAt: string; // 実測ISO8601
}

// 1回のcollectで得た一括スナップショット
export interface SnapshotBatch {
  readonly capturedAt: string;
  readonly channels: readonly ChannelSnapshot[];
  readonly videos: readonly VideoSnapshot[];
}
```

- [ ] **Step 7: `src/paths.ts` を実装**

```typescript
import { resolve } from "node:path";
import type { ResearchConfig } from "./config.js";

export interface ResearchPaths {
  readonly dataDir: string;
  readonly ledgerYaml: string;
  readonly snapshotsDir: string;
  readonly dbFile: string;
}

export function resolveResearchPaths(config: ResearchConfig): ResearchPaths {
  const dataDir = resolve(config.dataDir);
  return {
    dataDir,
    ledgerYaml: resolve(dataDir, "ledger.yaml"),
    snapshotsDir: resolve(dataDir, "snapshots"),
    dbFile: resolve(dataDir, "data.db")
  };
}

// スナップショット保存ディレクトリ名（コロンはファイル名に使えないため除去）
export function snapshotDirFor(config: ResearchConfig, capturedAt: string): string {
  const safe = capturedAt.replace(/:/g, "");
  return resolve(config.dataDir, "snapshots", safe);
}
```

- [ ] **Step 8: `config.test.ts` を書く**

```typescript
import { loadResearchConfig } from "../src/config.js";

describe("loadResearchConfig", () => {
  test("YOUTUBE_API_KEY未設定なら例外", () => {
    expect(() => loadResearchConfig({})).toThrow(/YOUTUBE_API_KEY/);
  });

  test("既定値を補完する", () => {
    const config = loadResearchConfig({ YOUTUBE_API_KEY: "k" });
    expect(config.youtubeBaseUrl).toBe("https://www.googleapis.com/youtube/v3");
    expect(config.dataDir).toBe("research-data");
    expect(config.logLevel).toBe("info");
  });
});
```

- [ ] **Step 9: テストと型チェックが緑であることを確認**

Run: `pnpm install && pnpm --filter @yancha/research test && pnpm --filter @yancha/research build`
Expected: paths / config テストが PASS、型エラーなし。

- [ ] **Step 10: Commit**

```bash
git add -A
git commit -m "feat: @yancha/researchパッケージの設定・型・パス解決を追加 (#13)"
```

---

## Task 3: 台帳（ledger）の読み書き

**Files:**
- Create: `packages/research/src/ledger.ts`
- Test: `packages/research/tests/ledger.test.ts`

**Interfaces:**
- Consumes: `LedgerEntry`（types.ts）, `ResearchPaths`（paths.ts）
- Produces:
  - `readLedger(ledgerYamlPath: string): Promise<LedgerEntry[]>`（ファイル無しなら空配列）
  - `addLedgerEntry(ledgerYamlPath: string, entry: LedgerEntry): Promise<LedgerEntry[]>`（重複 channelId はスキップ）
  - `parseChannelId(input: string): string`（URL または生ID から channelId を抽出）

- [ ] **Step 1: 失敗テストを書く**

```typescript
import { mkdtemp, readFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { readLedger, addLedgerEntry, parseChannelId } from "../src/ledger.js";

describe("parseChannelId", () => {
  test("channel URLからIDを抽出", () => {
    expect(parseChannelId("https://www.youtube.com/channel/UC1234567890abcdefghABCD")).toBe("UC1234567890abcdefghABCD");
  });
  test("生のIDはそのまま返す", () => {
    expect(parseChannelId("UC1234567890abcdefghABCD")).toBe("UC1234567890abcdefghABCD");
  });
});

describe("ledger 読み書き", () => {
  test("ファイルが無ければ空配列", async () => {
    const dir = await mkdtemp(join(tmpdir(), "ledger-"));
    expect(await readLedger(join(dir, "ledger.yaml"))).toEqual([]);
  });

  test("追加した内容を読み戻せる／重複はスキップ", async () => {
    const dir = await mkdtemp(join(tmpdir(), "ledger-"));
    const path = join(dir, "ledger.yaml");
    await addLedgerEntry(path, { channelId: "UCaaa", note: "睡眠系大手", tags: ["sleep"] });
    const after = await addLedgerEntry(path, { channelId: "UCaaa", note: "重複", tags: [] });
    expect(after).toHaveLength(1);
    const reloaded = await readLedger(path);
    expect(reloaded[0].channelId).toBe("UCaaa");
    expect(reloaded[0].note).toBe("睡眠系大手");
    expect((await readFile(path, "utf8")).length).toBeGreaterThan(0);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `pnpm --filter @yancha/research test ledger`
Expected: FAIL（`../src/ledger.js` が存在しない）

- [ ] **Step 3: `src/ledger.ts` を実装**

```typescript
import { mkdir, readFile, writeFile } from "node:fs/promises";
import { dirname } from "node:path";
import { parse, stringify } from "yaml";
import { YanchaError } from "@yancha/core";
import type { LedgerEntry } from "./types.js";

// URL または生IDから channelId(UC...) を取り出す
export function parseChannelId(input: string): string {
  const trimmed = input.trim();
  const match = trimmed.match(/channel\/(UC[\w-]{22})/);
  if (match) {
    return match[1];
  }
  if (/^UC[\w-]{22}$/.test(trimmed)) {
    return trimmed;
  }
  throw new YanchaError("CONFIG_INVALID", `channelIdを特定できません: ${input}（channel/UC... のURLか生のUC...IDを指定してください）`);
}

export async function readLedger(ledgerYamlPath: string): Promise<LedgerEntry[]> {
  let raw: string;
  try {
    raw = await readFile(ledgerYamlPath, "utf8");
  } catch (error) {
    if ((error as NodeJS.ErrnoException).code === "ENOENT") {
      return [];
    }
    throw error;
  }
  const parsed = parse(raw) as { channels?: LedgerEntry[] } | null;
  return parsed?.channels ?? [];
}

export async function addLedgerEntry(ledgerYamlPath: string, entry: LedgerEntry): Promise<LedgerEntry[]> {
  const current = await readLedger(ledgerYamlPath);
  if (current.some((e) => e.channelId === entry.channelId)) {
    return current; // 重複はスキップ
  }
  const next = [...current, entry];
  await mkdir(dirname(ledgerYamlPath), { recursive: true });
  await writeFile(ledgerYamlPath, stringify({ channels: next }), "utf8");
  return next;
}
```

- [ ] **Step 4: テストが緑であることを確認**

Run: `pnpm --filter @yancha/research test ledger`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: リサーチ手動シード台帳(YAML)の読み書きを実装 (#13)"
```

---

## Task 4: YouTube Data API v3 クライアント

**Files:**
- Create: `packages/research/src/youtube.ts`
- Test: `packages/research/tests/youtube.test.ts`

**Interfaces:**
- Consumes: `ChannelSnapshot`, `VideoSnapshot`（types.ts）
- Produces:
  - `YoutubeClient` インターフェース: `fetchChannels(ids)`, `fetchUploadsPlaylistId(channelId)`, `fetchRecentVideoIds(playlistId, max)`, `fetchVideos(ids)`
  - `createYoutubeClient(config, fetchImpl?)`: 実装。`fetchImpl` は差し替え可能（テスト用）
  - `parseIsoDurationToSeconds(iso: string): number`（ISO8601 duration → 秒）

- [ ] **Step 1: duration パースの失敗テストを書く**

```typescript
import { parseIsoDurationToSeconds, createYoutubeClient } from "../src/youtube.js";
import type { ResearchConfig } from "../src/config.js";

const config = {
  youtubeApiKey: "k",
  youtubeBaseUrl: "https://example.test/youtube/v3",
  dataDir: "research-data",
  logLevel: "info"
} satisfies ResearchConfig;

describe("parseIsoDurationToSeconds", () => {
  test("PT3H2M1S=10921秒", () => {
    expect(parseIsoDurationToSeconds("PT3H2M1S")).toBe(10921);
  });
  test("PT8H=28800秒", () => {
    expect(parseIsoDurationToSeconds("PT8H")).toBe(28800);
  });
  test("PT45S=45秒", () => {
    expect(parseIsoDurationToSeconds("PT45S")).toBe(45);
  });
});

describe("createYoutubeClient.fetchChannels", () => {
  test("APIキーを付与しstatistics/snippetをマップする", async () => {
    let calledUrl = "";
    const fakeFetch: typeof fetch = async (input) => {
      calledUrl = String(input);
      return new Response(JSON.stringify({
        items: [{
          id: "UCaaa",
          snippet: { title: "眠りチャンネル" },
          statistics: { subscriberCount: "1000", viewCount: "50000", videoCount: "42" }
        }]
      }), { status: 200 });
    };
    const client = createYoutubeClient(config, fakeFetch);
    const channels = await client.fetchChannels(["UCaaa"]);
    expect(calledUrl).toContain("key=k");
    expect(calledUrl).toContain("id=UCaaa");
    expect(channels[0]).toMatchObject({ channelId: "UCaaa", title: "眠りチャンネル", subscriberCount: 1000, viewCount: 50000, videoCount: 42 });
    expect(typeof channels[0].capturedAt).toBe("string");
  });

  test("HTTPエラーはYanchaErrorに変換", async () => {
    const fakeFetch: typeof fetch = async () => new Response("quota exceeded", { status: 403 });
    const client = createYoutubeClient(config, fakeFetch);
    await expect(client.fetchChannels(["UCaaa"])).rejects.toThrow(/403/);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `pnpm --filter @yancha/research test youtube`
Expected: FAIL（`../src/youtube.js` が存在しない）

- [ ] **Step 3: `src/youtube.ts` を実装**

```typescript
import { YanchaError } from "@yancha/core";
import type { ResearchConfig } from "./config.js";
import type { ChannelSnapshot, VideoSnapshot } from "./types.js";

export interface YoutubeClient {
  fetchChannels(channelIds: readonly string[]): Promise<ChannelSnapshot[]>;
  fetchUploadsPlaylistId(channelId: string): Promise<string>;
  fetchRecentVideoIds(playlistId: string, max: number): Promise<string[]>;
  fetchVideos(videoIds: readonly string[]): Promise<VideoSnapshot[]>;
}

// ISO8601 duration(PT#H#M#S) を秒に変換
export function parseIsoDurationToSeconds(iso: string): number {
  const match = iso.match(/^PT(?:(\d+)H)?(?:(\d+)M)?(?:(\d+)S)?$/);
  if (!match) {
    return 0;
  }
  const hours = Number(match[1] ?? 0);
  const minutes = Number(match[2] ?? 0);
  const seconds = Number(match[3] ?? 0);
  return hours * 3600 + minutes * 60 + seconds;
}

export function createYoutubeClient(config: ResearchConfig, fetchImpl: typeof fetch = fetch): YoutubeClient {
  async function getJson(path: string, params: Record<string, string>): Promise<any> {
    const url = new URL(`${config.youtubeBaseUrl}/${path}`);
    for (const [key, value] of Object.entries(params)) {
      url.searchParams.set(key, value);
    }
    url.searchParams.set("key", config.youtubeApiKey);
    const response = await fetchImpl(url);
    if (!response.ok) {
      const body = await response.text();
      throw new YanchaError("CLIENT_ERROR", `YouTube API エラー(${response.status}): ${body.slice(0, 200)}`);
    }
    return response.json();
  }

  return {
    async fetchChannels(channelIds) {
      if (channelIds.length === 0) {
        return [];
      }
      const capturedAt = new Date().toISOString();
      const json = await getJson("channels", { part: "snippet,statistics", id: channelIds.join(","), maxResults: "50" });
      return (json.items ?? []).map((item: any): ChannelSnapshot => ({
        channelId: item.id,
        title: item.snippet?.title ?? "",
        subscriberCount: Number(item.statistics?.subscriberCount ?? 0),
        viewCount: Number(item.statistics?.viewCount ?? 0),
        videoCount: Number(item.statistics?.videoCount ?? 0),
        capturedAt
      }));
    },

    async fetchUploadsPlaylistId(channelId) {
      const json = await getJson("channels", { part: "contentDetails", id: channelId });
      const playlistId = json.items?.[0]?.contentDetails?.relatedPlaylists?.uploads;
      if (!playlistId) {
        throw new YanchaError("CLIENT_ERROR", `アップロード用プレイリストが取得できません: ${channelId}`);
      }
      return playlistId;
    },

    async fetchRecentVideoIds(playlistId, max) {
      const json = await getJson("playlistItems", { part: "contentDetails", playlistId, maxResults: String(Math.min(max, 50)) });
      return (json.items ?? []).map((item: any) => item.contentDetails?.videoId).filter((id: unknown): id is string => typeof id === "string");
    },

    async fetchVideos(videoIds) {
      if (videoIds.length === 0) {
        return [];
      }
      const capturedAt = new Date().toISOString();
      const json = await getJson("videos", { part: "snippet,statistics,contentDetails", id: videoIds.join(","), maxResults: "50" });
      return (json.items ?? []).map((item: any): VideoSnapshot => ({
        videoId: item.id,
        channelId: item.snippet?.channelId ?? "",
        title: item.snippet?.title ?? "",
        publishedAt: item.snippet?.publishedAt ?? "",
        durationSeconds: parseIsoDurationToSeconds(item.contentDetails?.duration ?? "PT0S"),
        viewCount: Number(item.statistics?.viewCount ?? 0),
        tags: item.snippet?.tags ?? [],
        capturedAt
      }));
    }
  };
}
```

- [ ] **Step 4: テストが緑であることを確認**

Run: `pnpm --filter @yancha/research test youtube`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: YouTube Data API v3クライアント(1ユニット系のみ)を実装 (#14)"
```

---

## Task 5: ストレージ（`ResearchStore` インターフェース ＋ SQLite 実装）

**Files:**
- Create: `packages/research/src/store.ts`
- Test: `packages/research/tests/store.test.ts`

**Interfaces:**
- Consumes: `ChannelSnapshot`, `VideoSnapshot`, `SnapshotBatch`（types.ts）
- Produces:
  - `ResearchStore` インターフェース: `saveBatch(batch)`, `latestTwoChannelSnapshots(channelId)`, `allLatestChannelSnapshots()`, `latestVideoSnapshots()`, `close()`
  - `createSqliteStore(dbFile: string): ResearchStore`

- [ ] **Step 1: 失敗テストを書く**

```typescript
import { createSqliteStore } from "../src/store.js";
import type { SnapshotBatch } from "../src/types.js";

function batch(capturedAt: string, channelViews: number, videoViews: number): SnapshotBatch {
  return {
    capturedAt,
    channels: [{ channelId: "UCaaa", title: "眠り", subscriberCount: 1000, viewCount: channelViews, videoCount: 10, capturedAt }],
    videos: [{ videoId: "v1", channelId: "UCaaa", title: "湖畔の夜", publishedAt: "2026-07-01T00:00:00Z", durationSeconds: 3600, viewCount: videoViews, tags: ["sleep"], capturedAt }]
  };
}

describe("SQLite ResearchStore", () => {
  test("バッチを追記し最新2件を新しい順で取得できる", () => {
    const store = createSqliteStore(":memory:");
    store.saveBatch(batch("2026-07-14T00:00:00.000Z", 50000, 100));
    store.saveBatch(batch("2026-07-16T00:00:00.000Z", 50600, 220));
    const two = store.latestTwoChannelSnapshots("UCaaa");
    expect(two).toHaveLength(2);
    expect(two[0].capturedAt).toBe("2026-07-16T00:00:00.000Z");
    expect(two[0].viewCount).toBe(50600);
    expect(two[1].capturedAt).toBe("2026-07-14T00:00:00.000Z");
    store.close();
  });

  test("最新チャンネル・動画スナップショットを取得できる", () => {
    const store = createSqliteStore(":memory:");
    store.saveBatch(batch("2026-07-16T00:00:00.000Z", 50600, 220));
    expect(store.allLatestChannelSnapshots()[0].viewCount).toBe(50600);
    expect(store.latestVideoSnapshots()[0].durationSeconds).toBe(3600);
    store.close();
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `pnpm --filter @yancha/research test store`
Expected: FAIL（`../src/store.js` が存在しない）

- [ ] **Step 3: `src/store.ts` を実装**

```typescript
import Database from "better-sqlite3";
import type { ChannelSnapshot, VideoSnapshot, SnapshotBatch } from "./types.js";

// storage抽象。将来MariaDB等へ差し替える際はこのインターフェースを実装する
export interface ResearchStore {
  saveBatch(batch: SnapshotBatch): void;
  latestTwoChannelSnapshots(channelId: string): ChannelSnapshot[];
  allLatestChannelSnapshots(): ChannelSnapshot[];
  latestVideoSnapshots(): VideoSnapshot[];
  close(): void;
}

export function createSqliteStore(dbFile: string): ResearchStore {
  const db = new Database(dbFile);
  db.pragma("journal_mode = WAL");
  // 追記型: 更新せず取得時点の行を積む
  db.exec(`
    CREATE TABLE IF NOT EXISTS channel_snapshots (
      channel_id TEXT NOT NULL,
      title TEXT NOT NULL,
      subscriber_count INTEGER NOT NULL,
      view_count INTEGER NOT NULL,
      video_count INTEGER NOT NULL,
      captured_at TEXT NOT NULL
    );
    CREATE TABLE IF NOT EXISTS video_snapshots (
      video_id TEXT NOT NULL,
      channel_id TEXT NOT NULL,
      title TEXT NOT NULL,
      published_at TEXT NOT NULL,
      duration_seconds INTEGER NOT NULL,
      view_count INTEGER NOT NULL,
      tags TEXT NOT NULL,
      captured_at TEXT NOT NULL
    );
  `);

  const insertChannel = db.prepare(`INSERT INTO channel_snapshots (channel_id, title, subscriber_count, view_count, video_count, captured_at) VALUES (?, ?, ?, ?, ?, ?)`);
  const insertVideo = db.prepare(`INSERT INTO video_snapshots (video_id, channel_id, title, published_at, duration_seconds, view_count, tags, captured_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?)`);

  function mapChannelRow(row: any): ChannelSnapshot {
    return { channelId: row.channel_id, title: row.title, subscriberCount: row.subscriber_count, viewCount: row.view_count, videoCount: row.video_count, capturedAt: row.captured_at };
  }
  function mapVideoRow(row: any): VideoSnapshot {
    return { videoId: row.video_id, channelId: row.channel_id, title: row.title, publishedAt: row.published_at, durationSeconds: row.duration_seconds, viewCount: row.view_count, tags: JSON.parse(row.tags), capturedAt: row.captured_at };
  }

  return {
    saveBatch(batch) {
      const tx = db.transaction((b: SnapshotBatch) => {
        for (const c of b.channels) {
          insertChannel.run(c.channelId, c.title, c.subscriberCount, c.viewCount, c.videoCount, c.capturedAt);
        }
        for (const v of b.videos) {
          insertVideo.run(v.videoId, v.channelId, v.title, v.publishedAt, v.durationSeconds, v.viewCount, JSON.stringify(v.tags), v.capturedAt);
        }
      });
      tx(batch);
    },

    latestTwoChannelSnapshots(channelId) {
      const rows = db.prepare(`SELECT * FROM channel_snapshots WHERE channel_id = ? ORDER BY captured_at DESC LIMIT 2`).all(channelId);
      return rows.map(mapChannelRow);
    },

    allLatestChannelSnapshots() {
      // channelごとに最新capturedAtの行のみ
      const rows = db.prepare(`
        SELECT cs.* FROM channel_snapshots cs
        JOIN (SELECT channel_id, MAX(captured_at) AS mx FROM channel_snapshots GROUP BY channel_id) m
        ON cs.channel_id = m.channel_id AND cs.captured_at = m.mx
      `).all();
      return rows.map(mapChannelRow);
    },

    latestVideoSnapshots() {
      const rows = db.prepare(`
        SELECT vs.* FROM video_snapshots vs
        JOIN (SELECT video_id, MAX(captured_at) AS mx FROM video_snapshots GROUP BY video_id) m
        ON vs.video_id = m.video_id AND vs.captured_at = m.mx
      `).all();
      return rows.map(mapVideoRow);
    },

    close() {
      db.close();
    }
  };
}
```

- [ ] **Step 4: テストが緑であることを確認**

Run: `pnpm --filter @yancha/research test store`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: ResearchStore抽象とSQLite実装(追記型)を追加 (#14)"
```

---

## Task 6: collect（スナップショット取得 → 生JSON書き出し ＋ store追記）

**Files:**
- Create: `packages/research/src/collect.ts`
- Test: `packages/research/tests/collect.test.ts`

**Interfaces:**
- Consumes: `YoutubeClient`（youtube.ts）, `ResearchStore`（store.ts）, `LedgerEntry`（types.ts）, `SnapshotBatch`
- Produces:
  - `collectSnapshots(deps): Promise<SnapshotBatch>` — deps: `{ ledger: LedgerEntry[]; client: YoutubeClient; store: ResearchStore; snapshotsDir: string; maxVideosPerChannel?: number }`
  - 生JSON を `snapshotsDir/<capturedAtのコロン除去>/batch.json` に書き出す

- [ ] **Step 1: 失敗テストを書く**

```typescript
import { mkdtemp, readFile, readdir } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { collectSnapshots } from "../src/collect.js";
import { createSqliteStore } from "../src/store.js";
import type { YoutubeClient } from "../src/youtube.js";

const fakeClient: YoutubeClient = {
  async fetchChannels(ids) {
    return ids.map((id) => ({ channelId: id, title: "眠り", subscriberCount: 1000, viewCount: 50000, videoCount: 2, capturedAt: "2026-07-16T00:00:00.000Z" }));
  },
  async fetchUploadsPlaylistId() { return "PLuploads"; },
  async fetchRecentVideoIds() { return ["v1", "v2"]; },
  async fetchVideos(ids) {
    return ids.map((id) => ({ videoId: id, channelId: "UCaaa", title: "湖畔", publishedAt: "2026-07-01T00:00:00Z", durationSeconds: 3600, viewCount: 100, tags: ["sleep"], capturedAt: "2026-07-16T00:00:00.000Z" }));
  }
};

describe("collectSnapshots", () => {
  test("台帳を回してバッチを作りJSON書き出しとstore追記を行う", async () => {
    const dir = await mkdtemp(join(tmpdir(), "collect-"));
    const store = createSqliteStore(":memory:");
    const batch = await collectSnapshots({
      ledger: [{ channelId: "UCaaa", note: "", tags: [] }],
      client: fakeClient,
      store,
      snapshotsDir: dir
    });
    expect(batch.channels).toHaveLength(1);
    expect(batch.videos).toHaveLength(2);
    expect(store.allLatestChannelSnapshots()).toHaveLength(1);
    const subdirs = await readdir(dir);
    expect(subdirs).toHaveLength(1);
    const written = JSON.parse(await readFile(join(dir, subdirs[0], "batch.json"), "utf8"));
    expect(written.videos).toHaveLength(2);
    store.close();
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `pnpm --filter @yancha/research test collect`
Expected: FAIL（`../src/collect.js` が存在しない）

- [ ] **Step 3: `src/collect.ts` を実装**

```typescript
import { mkdir, writeFile } from "node:fs/promises";
import { join } from "node:path";
import type { YoutubeClient } from "./youtube.js";
import type { ResearchStore } from "./store.js";
import type { LedgerEntry, SnapshotBatch, VideoSnapshot } from "./types.js";

export interface CollectDeps {
  readonly ledger: readonly LedgerEntry[];
  readonly client: YoutubeClient;
  readonly store: ResearchStore;
  readonly snapshotsDir: string;
  readonly maxVideosPerChannel?: number;
}

export async function collectSnapshots(deps: CollectDeps): Promise<SnapshotBatch> {
  const capturedAt = new Date().toISOString();
  const maxVideos = deps.maxVideosPerChannel ?? 20;
  const channelIds = deps.ledger.map((e) => e.channelId);

  // チャンネル統計（channels.list = 1ユニット系）
  const channels = await deps.client.fetchChannels(channelIds);

  // 各チャンネルの最近動画IDを集める（playlistItems.list = 1ユニット系）
  const videoIds: string[] = [];
  for (const channelId of channelIds) {
    const playlistId = await deps.client.fetchUploadsPlaylistId(channelId);
    const ids = await deps.client.fetchRecentVideoIds(playlistId, maxVideos);
    videoIds.push(...ids);
  }

  // 動画統計（videos.list = 1ユニット系）。capturedAtをバッチ基準に揃える
  const rawVideos = await deps.client.fetchVideos(videoIds);
  const videos: VideoSnapshot[] = rawVideos.map((v) => ({ ...v, capturedAt }));
  const channelsFixed = channels.map((c) => ({ ...c, capturedAt }));

  const batch: SnapshotBatch = { capturedAt, channels: channelsFixed, videos };

  // 生JSONを保存（後から指標を追加再計算できるように残す）
  const safe = capturedAt.replace(/:/g, "");
  const outDir = join(deps.snapshotsDir, safe);
  await mkdir(outDir, { recursive: true });
  await writeFile(join(outDir, "batch.json"), `${JSON.stringify(batch, null, 2)}\n`, "utf8");

  // 時系列DBに追記
  deps.store.saveBatch(batch);
  return batch;
}
```

- [ ] **Step 4: テストが緑であることを確認**

Run: `pnpm --filter @yancha/research test collect`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: collectステージ(スナップショット取得と生JSON保存)を実装 (#14)"
```

---

## Task 7: エッジ指標の計算（経過時間で正規化）

**Files:**
- Create: `packages/research/src/metrics.ts`
- Test: `packages/research/tests/metrics.test.ts`

**Interfaces:**
- Consumes: `ChannelSnapshot`, `VideoSnapshot`（types.ts）
- Produces:
  - `channelViewVelocityPerDay(prev, curr): number | null`（2スナップショット間の views 増加 ÷ 実経過日数。経過0以下なら null）
  - `durationBuckets(videos): { label: string; count: number }[]`（尺の分布: <30m, 30-60m, 1-3h, 3-8h, 8h+）
  - `topWords(videos, limit): { word: string; count: number }[]`（タイトル＋タグの頻出語）

- [ ] **Step 1: 失敗テストを書く**

```typescript
import { channelViewVelocityPerDay, durationBuckets, topWords } from "../src/metrics.js";
import type { ChannelSnapshot, VideoSnapshot } from "../src/types.js";

function ch(capturedAt: string, viewCount: number): ChannelSnapshot {
  return { channelId: "UCaaa", title: "眠り", subscriberCount: 1000, viewCount, videoCount: 10, capturedAt };
}
function vid(durationSeconds: number, title: string, tags: string[]): VideoSnapshot {
  return { videoId: "v", channelId: "UCaaa", title, publishedAt: "2026-07-01T00:00:00Z", durationSeconds, viewCount: 1, tags, capturedAt: "2026-07-16T00:00:00.000Z" };
}

describe("channelViewVelocityPerDay", () => {
  test("2日で20000増なら1日1万", () => {
    const v = channelViewVelocityPerDay(ch("2026-07-14T00:00:00.000Z", 30000), ch("2026-07-16T00:00:00.000Z", 50000));
    expect(v).toBe(10000);
  });
  test("経過0以下ならnull", () => {
    expect(channelViewVelocityPerDay(ch("2026-07-16T00:00:00.000Z", 30000), ch("2026-07-16T00:00:00.000Z", 50000))).toBeNull();
  });
});

describe("durationBuckets", () => {
  test("尺をバケットに振り分ける", () => {
    const buckets = durationBuckets([vid(1200, "a", []), vid(7200, "b", []), vid(36000, "c", [])]);
    const map = Object.fromEntries(buckets.map((b) => [b.label, b.count]));
    expect(map["<30m"]).toBe(1);
    expect(map["1-3h"]).toBe(1);
    expect(map["8h+"]).toBe(1);
  });
});

describe("topWords", () => {
  test("タイトルとタグの頻出語を数える", () => {
    const words = topWords([vid(60, "睡眠 音楽", ["睡眠"]), vid(60, "睡眠 雨", ["雨"])], 5);
    const map = Object.fromEntries(words.map((w) => [w.word, w.count]));
    expect(map["睡眠"]).toBe(3);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `pnpm --filter @yancha/research test metrics`
Expected: FAIL（`../src/metrics.js` が存在しない）

- [ ] **Step 3: `src/metrics.ts` を実装**

```typescript
import type { ChannelSnapshot, VideoSnapshot } from "./types.js";

// 2スナップショット間の再生数増加を1日あたりに正規化。取得間隔がバラバラでも壊れない
export function channelViewVelocityPerDay(prev: ChannelSnapshot, curr: ChannelSnapshot): number | null {
  const elapsedMs = new Date(curr.capturedAt).getTime() - new Date(prev.capturedAt).getTime();
  if (elapsedMs <= 0) {
    return null;
  }
  const elapsedDays = elapsedMs / (1000 * 60 * 60 * 24);
  return (curr.viewCount - prev.viewCount) / elapsedDays;
}

const bucketDefs: readonly { label: string; maxSeconds: number }[] = [
  { label: "<30m", maxSeconds: 30 * 60 },
  { label: "30-60m", maxSeconds: 60 * 60 },
  { label: "1-3h", maxSeconds: 3 * 3600 },
  { label: "3-8h", maxSeconds: 8 * 3600 },
  { label: "8h+", maxSeconds: Number.POSITIVE_INFINITY }
];

export function durationBuckets(videos: readonly VideoSnapshot[]): { label: string; count: number }[] {
  const counts = new Map<string, number>(bucketDefs.map((b) => [b.label, 0]));
  for (const video of videos) {
    const bucket = bucketDefs.find((b) => video.durationSeconds < b.maxSeconds) ?? bucketDefs[bucketDefs.length - 1];
    counts.set(bucket.label, (counts.get(bucket.label) ?? 0) + 1);
  }
  return bucketDefs.map((b) => ({ label: b.label, count: counts.get(b.label) ?? 0 }));
}

export function topWords(videos: readonly VideoSnapshot[], limit: number): { word: string; count: number }[] {
  const counts = new Map<string, number>();
  for (const video of videos) {
    const words = [...video.title.split(/[\s　,、。|｜/]+/), ...video.tags];
    for (const raw of words) {
      const word = raw.trim();
      if (word.length === 0) {
        continue;
      }
      counts.set(word, (counts.get(word) ?? 0) + 1);
    }
  }
  return [...counts.entries()]
    .map(([word, count]) => ({ word, count }))
    .sort((a, b) => b.count - a.count)
    .slice(0, limit);
}
```

- [ ] **Step 4: テストが緑であることを確認**

Run: `pnpm --filter @yancha/research test metrics`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: 経過時間正規化のエッジ指標(伸び率/尺分布/頻出語)を実装 (#15)"
```

---

## Task 8: report（DBから集計を組み立て）

**Files:**
- Create: `packages/research/src/report.ts`
- Test: `packages/research/tests/report.test.ts`

**Interfaces:**
- Consumes: `ResearchStore`（store.ts）, `metrics.ts` の各関数
- Produces:
  - `ReportData`: `{ channels: { channelId; title; subscriberCount; viewCount; velocityPerDay: number | null }[]; durationBuckets; topWords }`
  - `buildReport(store: ResearchStore, topWordLimit?: number): ReportData`

- [ ] **Step 1: 失敗テストを書く**

```typescript
import { buildReport } from "../src/report.js";
import { createSqliteStore } from "../src/store.js";
import type { SnapshotBatch } from "../src/types.js";

function batch(capturedAt: string, channelViews: number): SnapshotBatch {
  return {
    capturedAt,
    channels: [{ channelId: "UCaaa", title: "眠り", subscriberCount: 1000, viewCount: channelViews, videoCount: 10, capturedAt }],
    videos: [{ videoId: "v1", channelId: "UCaaa", title: "睡眠 音楽", publishedAt: "2026-07-01T00:00:00Z", durationSeconds: 3600, viewCount: 100, tags: ["睡眠"], capturedAt }]
  };
}

describe("buildReport", () => {
  test("チャンネル伸び率・尺分布・頻出語を集計する", () => {
    const store = createSqliteStore(":memory:");
    store.saveBatch(batch("2026-07-14T00:00:00.000Z", 30000));
    store.saveBatch(batch("2026-07-16T00:00:00.000Z", 50000));
    const report = buildReport(store, 5);
    expect(report.channels[0].velocityPerDay).toBe(10000);
    expect(report.durationBuckets.find((b) => b.label === "1-3h")?.count).toBe(1);
    expect(report.topWords.find((w) => w.word === "睡眠")?.count).toBe(2);
    store.close();
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `pnpm --filter @yancha/research test report`
Expected: FAIL（`../src/report.js` が存在しない）

- [ ] **Step 3: `src/report.ts` を実装**

```typescript
import type { ResearchStore } from "./store.js";
import { channelViewVelocityPerDay, durationBuckets, topWords } from "./metrics.js";

export interface ReportChannelRow {
  readonly channelId: string;
  readonly title: string;
  readonly subscriberCount: number;
  readonly viewCount: number;
  readonly velocityPerDay: number | null;
}

export interface ReportData {
  readonly channels: ReportChannelRow[];
  readonly durationBuckets: { label: string; count: number }[];
  readonly topWords: { word: string; count: number }[];
}

export function buildReport(store: ResearchStore, topWordLimit = 20): ReportData {
  const latest = store.allLatestChannelSnapshots();
  const channels: ReportChannelRow[] = latest.map((curr) => {
    const two = store.latestTwoChannelSnapshots(curr.channelId);
    const velocityPerDay = two.length === 2 ? channelViewVelocityPerDay(two[1], two[0]) : null;
    return {
      channelId: curr.channelId,
      title: curr.title,
      subscriberCount: curr.subscriberCount,
      viewCount: curr.viewCount,
      velocityPerDay
    };
  });
  const videos = store.latestVideoSnapshots();
  return {
    channels,
    durationBuckets: durationBuckets(videos),
    topWords: topWords(videos, topWordLimit)
  };
}
```

- [ ] **Step 4: テストが緑であることを確認**

Run: `pnpm --filter @yancha/research test report`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: DBからエッジ指標レポートを組み立てるreportを実装 (#15)"
```

---

## Task 9: Webビュー（Hono サーバー ＋ 1画面）

**Files:**
- Create: `packages/research/src/server.ts`, `packages/research/src/web/index.html`
- Test: `packages/research/tests/server.test.ts`

**Interfaces:**
- Consumes: `ResearchStore`（store.ts）, `buildReport`（report.ts）
- Produces:
  - `createServer(store: ResearchStore): Hono` — ルート: `GET /`（HTML）, `GET /api/report`（`ReportData` JSON）
  - `startServer(store, port): void`（`serve` から使用。テストでは呼ばない）

- [ ] **Step 1: 失敗テストを書く（Hono の `app.request` を使い起動不要でテスト）**

```typescript
import { createServer } from "../src/server.js";
import { createSqliteStore } from "../src/store.js";
import type { SnapshotBatch } from "../src/types.js";

const batch: SnapshotBatch = {
  capturedAt: "2026-07-16T00:00:00.000Z",
  channels: [{ channelId: "UCaaa", title: "眠り", subscriberCount: 1000, viewCount: 50000, videoCount: 10, capturedAt: "2026-07-16T00:00:00.000Z" }],
  videos: [{ videoId: "v1", channelId: "UCaaa", title: "睡眠 音楽", publishedAt: "2026-07-01T00:00:00Z", durationSeconds: 3600, viewCount: 100, tags: ["睡眠"], capturedAt: "2026-07-16T00:00:00.000Z" }]
};

describe("createServer", () => {
  test("GET /api/report がReportData JSONを返す", async () => {
    const store = createSqliteStore(":memory:");
    store.saveBatch(batch);
    const app = createServer(store);
    const res = await app.request("/api/report");
    expect(res.status).toBe(200);
    const json = await res.json();
    expect(json.channels[0].channelId).toBe("UCaaa");
    expect(Array.isArray(json.durationBuckets)).toBe(true);
    store.close();
  });

  test("GET / がHTMLを返す", async () => {
    const store = createSqliteStore(":memory:");
    const app = createServer(store);
    const res = await app.request("/");
    expect(res.status).toBe(200);
    expect(res.headers.get("content-type")).toContain("text/html");
    store.close();
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `pnpm --filter @yancha/research test server`
Expected: FAIL（`../src/server.js` が存在しない）

- [ ] **Step 3: `src/web/index.html` を作成**

チャートは Chart.js（CDN）。1画面に「チャンネル一覧（伸び率つき表）」「尺分布（棒）」「頻出語（上位）」を置く。配色は落ち着いたダーク基調（睡眠系トーン）。

```html
<!doctype html>
<html lang="ja">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>リサーチ定点観測</title>
  <script src="https://cdn.jsdelivr.net/npm/chart.js@4"></script>
  <style>
    body { font-family: system-ui, sans-serif; background: #12151c; color: #e6e8ee; margin: 24px; }
    h1 { font-size: 18px; }
    table { border-collapse: collapse; width: 100%; margin-bottom: 32px; }
    th, td { border-bottom: 1px solid #2a2f3a; padding: 6px 10px; text-align: right; }
    th:first-child, td:first-child { text-align: left; }
    .grid { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; }
    canvas { background: #1a1e27; border-radius: 8px; padding: 8px; }
  </style>
</head>
<body>
  <h1>リサーチ定点観測（読み取りは人間が行う）</h1>
  <table id="channels"><thead><tr><th>チャンネル</th><th>登録者</th><th>総再生</th><th>伸び/日</th></tr></thead><tbody></tbody></table>
  <div class="grid">
    <canvas id="durations"></canvas>
    <canvas id="words"></canvas>
  </div>
  <script>
    async function main() {
      const report = await (await fetch("/api/report")).json();
      const tbody = document.querySelector("#channels tbody");
      for (const c of report.channels) {
        const tr = document.createElement("tr");
        const vel = c.velocityPerDay == null ? "—" : Math.round(c.velocityPerDay).toLocaleString();
        tr.innerHTML = `<td>${c.title}</td><td>${c.subscriberCount.toLocaleString()}</td><td>${c.viewCount.toLocaleString()}</td><td>${vel}</td>`;
        tbody.appendChild(tr);
      }
      new Chart(document.getElementById("durations"), {
        type: "bar",
        data: { labels: report.durationBuckets.map(b => b.label), datasets: [{ label: "尺の分布", data: report.durationBuckets.map(b => b.count), backgroundColor: "#5b8def" }] }
      });
      new Chart(document.getElementById("words"), {
        type: "bar",
        data: { labels: report.topWords.map(w => w.word), datasets: [{ label: "頻出語", data: report.topWords.map(w => w.count), backgroundColor: "#8a7de0" }] },
        options: { indexAxis: "y" }
      });
    }
    main();
  </script>
</body>
</html>
```

- [ ] **Step 4: `src/server.ts` を実装**

```typescript
import { readFile } from "node:fs/promises";
import { fileURLToPath } from "node:url";
import { dirname, join } from "node:path";
import { Hono } from "hono";
import { serve } from "@hono/node-server";
import type { ResearchStore } from "./store.js";
import { buildReport } from "./report.js";

const currentDir = dirname(fileURLToPath(import.meta.url));

export function createServer(store: ResearchStore): Hono {
  const app = new Hono();

  app.get("/api/report", (c) => {
    return c.json(buildReport(store));
  });

  app.get("/", async (c) => {
    const html = await readFile(join(currentDir, "web", "index.html"), "utf8");
    return c.html(html);
  });

  return app;
}

export function startServer(store: ResearchStore, port: number): void {
  const app = createServer(store);
  serve({ fetch: app.fetch, port });
  console.log(`[INFO] リサーチWebビューを起動しました: http://localhost:${port}`);
}
```

- [ ] **Step 5: テストが緑であることを確認**

Run: `pnpm --filter @yancha/research test server`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: リサーチWebビュー(Honoサーバー+1画面)を実装 (#15)"
```

---

## Task 10: CLI（`add` / `collect` / `serve`）と README・.env

**Files:**
- Create: `packages/research/src/cli.ts`, `packages/research/README.md`
- Modify: root `.env.example`（`YOUTUBE_API_KEY` 等を追記）, root `.gitignore`（`research-data/` を無視）

**Interfaces:**
- Consumes: これまでの全モジュール（config/paths/ledger/youtube/collect/store/server）

- [ ] **Step 1: `src/cli.ts` を実装**

```typescript
import { YanchaError, toErrorMessage, Logger } from "@yancha/core";
import { loadResearchConfig } from "./config.js";
import { resolveResearchPaths } from "./paths.js";
import { readLedger, addLedgerEntry, parseChannelId } from "./ledger.js";
import { createYoutubeClient } from "./youtube.js";
import { createSqliteStore } from "./store.js";
import { collectSnapshots } from "./collect.js";
import { startServer } from "./server.js";

async function main(): Promise<void> {
  const args = process.argv.slice(2);
  const command = args[0];
  const config = loadResearchConfig();
  const paths = resolveResearchPaths(config);
  const logger = new Logger(config.logLevel);

  if (command === "add") {
    const url = args[1];
    if (!url) {
      throw new YanchaError("CONFIG_INVALID", "使い方: research add <channel_url|channel_id> [メモ]");
    }
    const channelId = parseChannelId(url);
    const note = args[2] ?? "";
    const after = await addLedgerEntry(paths.ledgerYaml, { channelId, note, tags: [] });
    logger.info("台帳に追加しました", { channelId, 件数: after.length });
    return;
  }

  if (command === "collect") {
    const ledger = await readLedger(paths.ledgerYaml);
    if (ledger.length === 0) {
      throw new YanchaError("CONFIG_INVALID", "台帳が空です。まず research add でチャンネルを登録してください。");
    }
    const client = createYoutubeClient(config);
    const store = createSqliteStore(paths.dbFile);
    try {
      const batch = await collectSnapshots({ ledger, client, store, snapshotsDir: paths.snapshotsDir });
      logger.info("スナップショットを取得しました", { capturedAt: batch.capturedAt, channels: batch.channels.length, videos: batch.videos.length });
    } finally {
      store.close();
    }
    return;
  }

  if (command === "serve") {
    const port = Number(args[1] ?? 5177);
    const store = createSqliteStore(paths.dbFile);
    startServer(store, port);
    // serveは常駐せず、見たい時に起動して手動で停止(Ctrl+C)する
    return;
  }

  throw new YanchaError("CONFIG_INVALID", "コマンドを指定してください: add / collect / serve");
}

main().catch((error: unknown) => {
  const prefix = error instanceof YanchaError ? `[${error.code}] ` : "";
  console.error(`${prefix}${toErrorMessage(error)}`);
  process.exitCode = 1;
});
```

- [ ] **Step 2: root `.env.example` に追記**

既存の末尾に以下を追記:

```
# リサーチ定点観測
YOUTUBE_API_KEY=
YOUTUBE_BASE_URL=https://www.googleapis.com/youtube/v3
YANCHA_RESEARCH_DIR=research-data
```

- [ ] **Step 3: root `.gitignore` に追記**

`research-data/` を追記（生成データ・SQLite・生JSONはコミットしない）。

- [ ] **Step 4: `packages/research/README.md` を作成**

```markdown
# @yancha/research — リサーチ定点観測

手動シードした競合チャンネルをコマンド駆動でスナップショット取得し、
経過時間で正規化したエッジ指標をローカルWebビューで確認する。常駐しない。

## 前提
- `YOUTUBE_API_KEY`（YouTube Data API v3）を `.env` に設定。
- `search.list` は使わない（1ユニット系APIのみ）。

## 使い方
```bash
pnpm --filter @yancha/research research add <channel_url|channel_id> "メモ"
pnpm --filter @yancha/research research collect   # 「今」のスナップショットを撮る
pnpm --filter @yancha/research research serve      # http://localhost:5177 を開く
```

判定は人間が行う。Webビューは指標を提示するだけ。
```

- [ ] **Step 5: 動作確認（型チェック・全テスト）**

Run: `pnpm install && pnpm --filter @yancha/research build && pnpm --filter @yancha/research test`
Expected: 型エラーなし、全テスト PASS。

- [ ] **Step 6: CLIスモークテスト（APIキー不要な範囲）**

Run: `YOUTUBE_API_KEY=dummy pnpm --filter @yancha/research research add https://www.youtube.com/channel/UC1234567890abcdefghABCD "テスト"`
Expected: `台帳に追加しました` のログが出て `research-data/ledger.yaml` が生成される。確認後 `research-data/` は削除してよい。

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat: リサーチCLI(add/collect/serve)とREADME・env設定を追加 (#13 #14 #15)"
```

---

## 完了時の確認（全タスク後）

- [ ] `pnpm test`（root）と `pnpm --filter @yancha/research test` が両方 PASS。
- [ ] `pnpm build` と `pnpm --filter @yancha/research build` が型エラーなし。
- [ ] Issue #13 / #14 / #15 を、対応コミットで参照済み（クローズは人間が最終確認後）。
- [ ] 後続 #16(プランB) / #17(ダッシュボード) / #18(プランC) は本計画のスコープ外（別計画）。
